- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zprofile:
    ~/.zshrc:
    ~/.gitignore_global:
    ~/.gitconfig:

- create:
    - ~/downloads
    - ~/.vim/undo-history
